# SHARED config — every service inherits this (filename 'application' = applies to all).
# Where the Eureka registry lives, so services don't each hardcode it.
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
